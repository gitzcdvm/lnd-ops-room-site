# Copy this file to .env.local and fill in the two values.
# The app reads these two names only (see app/lib/supabase.ts).
# Find them in your Supabase project: Project Settings, API.
#
# Never commit .env.local itself. For a deploy, set both names in
# Vercel: Project, Settings, Environment Variables (Production, Preview
# and Development), then redeploy.

NEXT_PUBLIC_SUPABASE_URL=https://htlluzeijurhzpjobrsd.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_uubzpK3-7kHYpO-EsHhCJw_UO4-HbPf
